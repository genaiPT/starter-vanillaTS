{
  "name": "ts-projects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"tsc",
    "dev":"tsc --watch",
    "log":"node --watch dist/index.js"

  },
  "exclude": ["src/**/*.ts", "node_modules"],
  "include": ["dist/**/*.js"],
  "author": "",
  "license": "ISC",

    "devDependencies": {
    "typescript": "^5.3.3"
  }
}
