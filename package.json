{
  "name": "ts-projects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "tailwind":"tailwindcss -i src/tailwind.css -o dist/css/styles.css --watch",
    "dev": "npm run tailwind && tsc --watch"

  },
  "exclude": [
    "src/**/*.ts"
  ],
  "include": [
    "dist/**/*.js"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.33",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3"
  }
}

